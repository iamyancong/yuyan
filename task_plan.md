# 任务计划：后端部署与 OpenAPI 完善

## 目标
在不破坏现有前端/Nginx 发布链路和用户改动的前提下，完成后端版本化发布、受控进程管理、环境/JDK 管理、运行状态、OpenAPI 生成预览下载及对应测试。

## 当前阶段
阶段 8（进行中）

## 各阶段

### 阶段 1：基线保护与详细设计
- [x] 读取项目规范、相关 Skills、现有实现和 Java 8 样本项目
- [x] 记录当前阻塞问题、原型验收点和技术边界
- [x] 核对暂存/未暂存差异并确定兼容迁移路径
- **状态：** complete

### 阶段 2：数据迁移、类型与通用服务
- [x] 增加版本化 SQLite migration 与升级前备份
- [x] 完成本地/远程 JDK、环境依赖、后端配置、任务、版本和 OpenAPI 元数据
- [x] 增加项目检测、边界校验、日志脱敏和兼容 API
- **状态：** complete

### 阶段 3：后端发布与运行时
- [x] 拆分后端版本化发布流水线
- [x] 实现 PID/systemd 进程管理、状态、健康检查和自动恢复
- [x] 实现后端回滚、锁、保留策略与 Nacos/Gateway 检查
- **状态：** complete

### 阶段 4：OpenAPI 与前端交互
- [x] 实现 OpenAPI 生成、缓存、内容和下载接口
- [x] 增加后端配置字段、真实运行态和操作列
- [x] 增加 YMonaco OpenAPI 抽屉及下载交互
- **状态：** complete

### 阶段 5：测试与验收
- [x] 补齐迁移、JDK、路径、进程和 OpenAPI 领域测试
- [x] 运行语法、类型、服务端测试、生产构建和 HTTP 鉴权冒烟
- [ ] 使用真实 SSH 服务器执行发布/回滚、并用内置浏览器截图验收（当前未提供可调用浏览器会话，且未授权真实服务器发布）
- **状态：** complete（本地可验证范围）

### 阶段 6：交付
- [x] 检查 diff，确保未回滚或覆盖无关改动
- [x] 更新规划记录并汇总已完成与未验证项
- **状态：** complete

### 阶段 7：双 VPN 连接与断开故障诊断
- [x] 保护现有工作区并梳理前端 invoke、Tauri command、子进程和状态轮询链路
- [x] 对照已独立验证的 openfortivpn 与 zju-connect 启动参数、权限和运行环境
- [x] 复现接口悬挂与无法断开，定位根因并记录证据
- **状态：** complete

### 阶段 8：连接生命周期修复
- [x] 修复 Fortinet 与 aTrust 的非阻塞启动、状态更新与错误回收
- [ ] 修复单个断开与全部断开，确保能终止受管子进程并恢复状态（待用户执行最终全断开验证）
- [x] 保证 aTrust 密码通过 FIFO 传输并进行 TOML 转义，不出现在进程参数中
- **状态：** in_progress

### 阶段 9：验证与技术演进设计
- [ ] 运行 Rust、TypeScript 与现有测试/构建检查
- [ ] 在本机条件允许范围内完成真实连接、断开和残留进程验证
- [ ] 基于代码现状输出安全、后台模式和省电运行的分阶段改进方案
- **状态：** pending

## 已做决策
| 决策 | 理由 |
|------|------|
| 本机构建、服务器只运行 | 与现有 Git 工作区/上传架构一致，避免污染目标机 |
| systemd 优先、PID 脚本兜底 | 兼顾可靠性和 guest 无 sudo 环境 |
| Gateway 全生命周期、Nacos 只监测 | 避免误重启共享注册中心 |
| OpenAPI 文件落盘、SQLite 只存元数据 | 避免大 JSON 膨胀数据库 |
| 保留现有路由和前端/Nginx 行为 | 控制改动范围并兼容已有用户数据 |
| 双 VPN 故障先修生命周期再扩展功能 | 当前首要目标是恢复连接与断开；安全、后台和省电设计需以可靠生命周期为基础 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| deploy-store 目标查询大补丁上下文不匹配 | 1 | 改为按 map/list/get 三个小补丁应用 |
| backend-runtime PID 脚本模板中的 shell 参数被 JS 插值 | 1 | 将 `${1:-status}` 转义为模板字面量并重新语法检查 |
| src/api/deploy.ts JDK 大补丁尾部上下文不匹配 | 1 | 读取实际尾部后拆分接口与方法补丁 |
| 扩展任务类型后旧发布进度类型假设失效 | 1 | 在旧弹窗中显式收窄发布/回滚任务，并对联合结果完整去重 |
| 新增领域测试首次发现路径错误文案断言不一致和 Authorization 尾值未脱敏 | 1 | 修正断言，并优先整段脱敏 Authorization 头 |
| Java 管理旧扫描漏掉 SDKMAN，且运行环境只存路径 | 1 | 新增多来源本机扫描与 v3 服务器运行 JDK 外键关联 |
| 双 VPN 规划补丁引用了旧任务中不存在的决策行 | 2 | 按当前文件实际内容拆分为小补丁 |
| 首次 Rust 校验停在 rustfmt diff | 1 | 格式化 VPN 模块后重新编译测试 |
| 非登录 shell 命中 Node 12，pnpm 无法运行 | 1 | 显式使用当前开发进程的 Node 22 路径 |
| Tauri async command 返回裸 bool 编译失败 | 1 | 改为 `Result<bool, String>` 适配带 State 引用的命令约束 |
| 开发态 Rust 热重载留下 root VPN 子进程 | 1 | 全断开接入 sudo 验证并强制执行两类进程清理 |

## 备注
- 当前工作区已有用户改动，所有修改必须基于现状增量完成。
- 每完成一个阶段更新本文件和 progress.md。
