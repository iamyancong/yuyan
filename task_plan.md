# 任务计划：后端部署、OpenAPI 与自动更新完善

## 目标
在保留既有部署与自动更新缓存架构的前提下，完成服务端预热和本机预下载全程静默、签名更新包就绪后再展示胶囊、用户确认后自动覆盖安装并重启，同时补齐持久化恢复与异常边界。

## 当前阶段
阶段 66（进行中）

## 各阶段

### 阶段 1：基线保护与详细设计
- [x] 读取项目规范、相关 Skills、现有实现和 Java 8 样本项目
- [x] 记录当前阻塞问题、原型验收点和技术边界
- [x] 核对暂存/未暂存差异并确定兼容迁移路径
- **状态：** complete

### 阶段 2：数据迁移、类型与通用服务
- [x] 增加版本化 SQLite migration 与升级前备份
- [x] 完成本地/远程 JDK、环境依赖、后端配置、任务、版本和 OpenAPI 元数据
- [x] 增加项目检测、边界校验、日志脱敏和兼容 API
- **状态：** complete

### 阶段 3：后端发布与运行时
- [x] 拆分后端版本化发布流水线
- [x] 实现 PID/systemd 进程管理、状态、健康检查和自动恢复
- [x] 实现后端回滚、锁、保留策略与 Nacos/Gateway 检查
- **状态：** complete

### 阶段 4：OpenAPI 与前端交互
- [x] 实现 OpenAPI 生成、缓存、内容和下载接口
- [x] 增加后端配置字段、真实运行态和操作列
- [x] 增加 YMonaco OpenAPI 抽屉及下载交互
- **状态：** complete

### 阶段 5：测试与验收
- [x] 补齐迁移、JDK、路径、进程和 OpenAPI 领域测试
- [x] 运行语法、类型、服务端测试、生产构建和 HTTP 鉴权冒烟
- [ ] 使用真实 SSH 服务器执行发布/回滚、并用内置浏览器截图验收（当前未提供可调用浏览器会话，且未授权真实服务器发布）
- **状态：** complete（本地可验证范围）

### 阶段 6：交付
- [x] 检查 diff，确保未回滚或覆盖无关改动
- [x] 更新规划记录并汇总已完成与未验证项
- **状态：** complete

### 阶段 7：自动更新基线与详细设计
- [x] 复核客户端、内网 API、GitHub Actions 和双分支职责
- [x] 明确主动预热、单任务回源、缓存状态与失败兜底协议
- [x] 核对免费 Tauri Updater 接入条件与密钥边界
- **状态：** complete

### 阶段 8：服务端主动预热与单任务缓存
- [x] 将预下载集合升级为带状态、进度和 Promise 的任务 Map
- [x] 实现服务端 `.part`、Range/If-Range 续传、指数退避和原子落盘
- [x] 增加定时 Release 检查并主动预热受支持平台安装包
- [x] 调整新客户端下载接口，缓存准备中返回 202；旧客户端保留迁移代理兜底
- **状态：** complete

### 阶段 9：客户端缓存就绪交互
- [x] 扩展更新检查类型与缓存状态接口
- [x] 缓存准备中展示加速准备状态并轮询，ready 后启动原生下载
- [x] 保留暂停、恢复、Range、进度、校验和安装兜底
- **状态：** complete

### 阶段 10：免费验签更新接入评估与实现
- [x] 检查现有 Tauri 密钥、GitHub Secrets 名称和构建产物条件
- [x] 确认现有依赖、动态清单和发布脚本可保留，但在缺少稳定密钥托管时不启用官方 Updater
- [x] 保留旧客户端到新缓存机制的兼容路径
- **状态：** complete（安全可实施范围）

### 阶段 11：测试与交付
- [x] 补齐缓存损坏、单任务、Range/If-Range、202/ready 与格式校验测试
- [x] 运行服务端测试、类型检查、Rust 测试和生产构建
- [x] 检查 diff、规划文件和双分支职责，不自动提交或推送
- **状态：** complete

### 阶段 12：静默更新状态机与验收设计
- [x] 复核当前 Vue、Tauri、服务端缓存协议与现有测试基线
- [x] 明确自动检查、手动检查、服务端预热、本机预下载、就绪安装和失败退避状态
- [x] 定义胶囊可见性、文案、用户授权边界与版本切换规则
- **状态：** complete

### 阶段 13：原生更新包持久化与版本绑定
- [x] 将本机下载状态绑定版本、assetId、ETag 和文件名
- [x] 支持 App 重启后恢复已完成安装包和断点状态
- [x] 增加旧安装包清理、空间检查、安装前完整性复核和过期资源淘汰
- **状态：** complete

### 阶段 14：前端静默预下载编排
- [x] 自动检查和服务器预热阶段隐藏胶囊
- [x] 服务器 ready 后自动启动本机静默下载，完成校验后才展示胶囊
- [x] 下载完成不自动安装，用户点击后才拉起安装并退出
- [x] 区分自动失败静默退避与手动检查即时反馈
- **状态：** complete

### 阶段 15：交互文案与异常边界
- [x] 胶囊展示目标版本及“已就绪，点击安装”语义
- [x] 处理缓存 failed/missing、轮询超时、断网恢复、连续新版本和旧任务冲突
- [x] 保留用户手动检查、下载进度查询与安装失败重试入口
- **状态：** complete

### 阶段 16：测试与交付
- [x] 补齐 Rust 状态恢复/版本绑定/清理测试及前端可测纯逻辑
- [x] 运行类型检查、服务端测试、Rust 测试、生产构建和 diff 检查
- [x] 更新规划记录并汇总行为变化与未覆盖的平台限制
- **状态：** complete

### 阶段 17：左侧菜单切换性能基线与根因定位
- [x] 复核 LayoutSider、BasicLayout、路由配置与页面挂载链路
- [x] 区分既有未提交改动与本次可安全优化范围
- [x] 通过运行时观察确认主线程阻塞、动画或重复请求的主要来源
- **状态：** complete

### 阶段 18：交互与渲染性能优化
- [x] 优化菜单即时反馈与路由视图切换策略
- [x] 避免无效重渲染、同步重活或过度过渡动画
- [x] 保持现有页面状态、路由语义和用户改动兼容
- **状态：** complete

### 阶段 19：验证与交付
- [x] 运行类型检查、生产构建和针对性交互验收
- [x] 检查 diff，确认未覆盖无关改动
- [x] 总结根因、优化行为和剩余边界
- **状态：** complete

### 阶段 20：四项问题基线与根因定位
- [x] 定位全局消息提示、后端构建命令/日志、部署表单和 JDK 架构检测实现
- [x] 复现或基于持久化任务数据还原后端构建失败链路
- [x] 将用户截图拆解为视觉与交互验收清单
- **状态：** complete

### 阶段 21：全局交互与部署链路修复
- [x] 治理频繁点击导致的重复消息，覆盖可复用的全局入口
- [x] 补齐后端构建失败的有效日志、错误摘要和可诊断信息
- [x] 修复部署表单 label 截断/遮挡并保持响应式布局
- [x] 明确并校验本机构建 JDK 与目标运行 JDK 的系统/CPU 架构边界
- **状态：** complete

### 阶段 22：验证与视觉验收
- [x] 运行针对性服务端测试、类型检查和生产构建
- [x] 启动页面并截图核对表单 label、后端发布历史和失败日志展示
- [x] 检查 diff，确认没有覆盖既有改动
- **状态：** complete

### 阶段 23：交付
- [x] 汇总四项根因、修复结果、数据证据和仍需真实发布验证的边界
- **状态：** complete

### 阶段 24：中央 API 边界纠正
- [x] 撤回后端发布历史列表按项目类型切换本地 API 的逻辑
- [x] 撤回后端发布记录详情读取本地 API 的逻辑
- [x] 保持后端特殊发布构建与 OpenAPI 生成的本地执行边界不变
- **状态：** complete

### 阶段 25：纠正后验证
- [x] 运行类型检查、服务端测试与生产构建
- [x] 检查 diff，确认历史/详情始终通过中央 API 客户端
- **状态：** complete

### 阶段 26：OpenAPI 同步后 JDK 失效根因
- [x] 还原同步测试环境配置到本地后的 OpenAPI 生成链路
- [x] 核对本地目标绑定、占位 JDK 与手动扫描后的真实 JDK 数据
- [x] 将截图报错转换为同步、自动扫描和生成验收项
- **状态：** complete

### 阶段 27：本机 JDK 自动修复
- [x] 同步 OpenAPI 配置时扫描本机 JDK 并按 Java 主版本绑定真实记录
- [x] 禁止把测试环境 buildJdkId 直接作为本机 JDK 绑定
- [x] OpenAPI 生成端检测路径和状态，失效时自动重扫再匹配
- **状态：** complete

### 阶段 28：自动修复验证
- [x] 运行类型检查、服务端测试与生产构建
- [x] 使用本地数据验证占位 JDK 会被真实 Java 8 替换
- [x] 检查视觉和交互，确认无需手动打开 Java 管理抽屉扫描
- **状态：** complete

### 阶段 29：OpenAPI JSON 高亮回归修复
- [x] 检查 YMonaco 最新文档、OpenAPI 预览配置与最近差异
- [x] 恢复 JSON 语法高亮且保持现有暗色预览布局
- [x] 运行类型检查并截图核对 key、字符串、数字和标点分色
- **状态：** complete

### 阶段 30：侧栏品牌与菜单视觉基线
- [x] 读取现有 LayoutSider、LogoSwift、主题变量与导航实现
- [x] 将用户截图拆解为 Logo、品牌文案、菜单文字、选中态和折叠控件验收项
- [x] 确定兼容亮/暗主题与 Tauri 标题栏的 C4D 玻璃拟态设计方案
- **状态：** complete

### 阶段 31：侧栏视觉升级实现
- [x] 升级可缩放雨燕 Logo 的体积、玻璃材质与主题适配
- [x] 重构品牌区、菜单项层级、图标容器和微交互
- [x] 将默认折叠触发器替换为有明确语义的玻璃胶囊控件
- **状态：** complete

### 阶段 32：构建与视觉验收
- [x] 运行类型检查、生产构建和 diff 检查
- [x] 在亮色、暗色、展开、折叠状态下截图核对布局与可读性
- [x] 汇总升级点和可继续延展的视觉区域
- **状态：** complete

### 阶段 33：侧栏减法与 Header 统一基线
- [x] 读取用户反馈截图、现有侧栏样式与 Header 实现
- [x] 确认选中态左光条/右光点重复，以及折叠态双层套壳根因
- [x] 明确侧栏与 Header 的统一表面、边界和阴影层级
- **状态：** complete

### 阶段 34：侧栏与 Header 精修实现
- [x] 去除菜单选中态两端标记并保留单一面高亮
- [x] 减薄 Logo 品牌卡与折叠控件边框、阴影，消除折叠态双层套壳
- [x] 将 Header 改为与侧栏一致的轻玻璃底色并兼容亮暗主题
- **状态：** complete

### 阶段 35：构建与多状态视觉复验
- [x] 运行类型检查、生产构建和 diff 检查
- [x] 截图核对亮色展开/折叠、Header 接缝及暗色状态
- [x] 汇总本轮减法结果并保护其他未提交改动
- **状态：** complete

### 阶段 36：菜单图标与折叠按钮几何精修
- [x] 将亮色菜单图标边界统一为指定的低对比 1px 灰色，不随选中态变紫
- [x] 强制折叠态底部按钮等宽高、单层圆角方形
- [x] 运行静态检查并截图复验展开/折叠状态
- **状态：** complete

### 阶段 37：自动替换与重启基线对照
- [x] 读取 `yuyan-vpn` 更新、安装、退出和重启实现及其打包配置
- [x] 复核 `yuyan-app` 当前原生下载器、安装入口、前端状态机和 Release 产物
- [x] 明确 Windows/macOS 的可迁移能力、权限边界与验收标准
- **状态：** complete

### 阶段 38：自动替换与重启方案设计
- [x] 确定复用 `yuyan-vpn` 方案或采用 Tauri 官方 Updater 的最小安全路径
- [x] 设计安装器参数、进程退出/重启时序、失败回退与旧客户端兼容策略
- [x] 明确签名、CI 产物和动态更新清单是否需要同步调整
- **状态：** complete

### 阶段 39：实现更新后自动替换并重启
- [x] 修改原生安装/重启链路并保持现有断点、校验和持久化能力
- [x] 调整前端安装状态与用户提示，避免重复点击和错误退出
- [x] 补充签名打包配置、动态清单、CI 产物门禁和针对性测试
- **状态：** complete

### 阶段 40：验证与交付
- [x] 运行 Rust 测试、Vue 类型检查、服务端测试和生产构建
- [x] 检查安装命令、退出时序、平台分支及 `git diff`
- [x] 汇总已实现行为、平台限制和真实安装验证步骤，不自动提交或推送
- **状态：** complete

### 阶段 41：生成 yuyan-app 专用 Updater 密钥
- [x] 生成独立加密私钥与配套公钥，私钥不进入仓库或日志
- [x] 将密钥密码安全保存到 macOS 钥匙串
- [x] 使用新密钥执行本地产物签名与公钥验签
- **状态：** complete

### 阶段 42：配置 GitHub Actions Secrets 与客户端公钥
- [x] 将专用私钥和密码写入 `ycwang-dev/yuyan` Actions Secrets
- [x] 更新客户端 Tauri 公钥；内网分支在客户端提交后通过 cherry-pick 同步
- [x] 提交并推送客户端修复，触发新一轮三平台构建
- **状态：** complete

### 阶段 43：CI 与 Release 验证
- [x] 监控 prep、Windows、macOS ARM/Intel、release jobs
- [x] 验证 Release 包含安装包、Updater、签名和 `latest.json`
- [x] 确认产物签名与客户端内置公钥一致
- **状态：** complete

### 阶段 44：双分支发布闭环
- [x] 将最终公钥修复同步到 `yuyan-3.0`
- [x] 推送内网分支并确认 GitLab 分支职责未变化
- [x] 清理临时 worktree，核对两个远端提交与主工作区状态
- **状态：** complete

### 阶段 45：Windows 控制台闪窗基线与调用面盘点
- [x] 确认 release 主进程已使用 Windows GUI 子系统，不是雨燕主窗口自身创建控制台
- [x] 盘点 Rust 启动 Node、运行时检查、taskkill 与 Node 服务内部 Git/Java/Maven/脚手架子进程
- [x] 确认闪窗来自后台控制台子进程未设置 Windows 隐藏窗口标志，且启动重试会放大闪烁次数
- **状态：** complete

### 阶段 46：后台子进程无窗口治理
- [x] 为 Rust 后台命令统一应用 Windows `CREATE_NO_WINDOW`
- [x] 为 Node 服务子进程统一应用 `windowsHide: true`
- [x] 将模板 Git 命令从 shell 字符串改为 `execFile` 参数调用，避免额外启动 `cmd.exe`
- [x] 增加后台子进程选项单元测试并复查全部运行时调用点
- **状态：** complete

### 阶段 47：构建验证与 Windows 验收边界
- [x] 完成 Rust 格式、编译与测试
- [x] 完成服务端测试、前端类型检查与生产构建
- [x] 完成 diff 检查，并记录真实 Windows 安装运行的人工验收项
- **状态：** complete

### 阶段 48：AI 控制平面基线与契约
- [x] 核对 Express、SQLite、Tauri 启动链路、部署服务与设置页复用边界
- [x] 固化 Agent Gateway、运行时描述文件、MCP 工具 Schema 与风险模型
- [x] 明确一期禁止能力、中央/本地执行位置和凭据隔离边界
- **状态：** complete

### 阶段 49：Agent Gateway、安全存储与异步任务
- [x] 实现 Loopback Token 鉴权、项目真实路径授权、配置计划和短期 planId
- [x] 实现审批、幂等、异步任务、取消、参数哈希绑定和 HMAC 审计链
- [x] 复用现有项目配置、脚手架、部署、回滚、服务与 OpenAPI 领域能力
- **状态：** complete

### 阶段 50：MCP Sidecar 与 Tauri `--mcp`
- [x] 实现稳定版 TypeScript MCP SDK stdio Server 和严格 Zod 工具输入
- [x] 实现运行时发现、桌面端自动拉起、stdout 纯净和 operation 轮询
- [x] 实现 Tauri 运行时描述文件、会话令牌轮换及退出清理
- **状态：** complete

### 阶段 51：AI 集成设置、安装与审批界面
- [x] 实现 Codex、Cursor、Antigravity 原子安装、备份、修复和卸载
- [x] 实现项目授权、待审批差异、任务进度、日志和审计历史界面
- [x] 实现审批唤醒与后台执行闭环，不要求 MCP 连接持续占用
- **状态：** complete

### 阶段 52：自动化测试与接入文档
- [x] 覆盖 Schema、脱敏、真实路径、令牌、客户端隔离授权、幂等和审计链
- [x] 使用模拟执行覆盖配置、发布、回滚、服务启停、取消和失败场景
- [x] 补充 MCP Inspector、三客户端配置及一期安全边界文档
- **状态：** complete

### 阶段 53：全量验证与交付
- [x] 执行 MCP、服务端、Vue、Vite、Rust 格式/编译/测试和 diff 检查
- [x] 核对 AppSplash 用户改动、打包资源、运行时路径与升级兼容
- [x] 记录真实测试环境发布闭环和跨平台安装的人工验收边界
- **状态：** complete

### 阶段 54：可信项目策略与路径基线
- [x] 核对项目授权、审批状态机、工具结果、客户端安装器和删除领域接口
- [x] 定义默认免审批边界、破坏性强制审批范围和开发/正式路径迁移规则
- [x] 将用户截图拆为策略开关、执行说明、路径提示和删除确认验收项
- **状态：** complete

### 阶段 55：策略状态机与破坏性工具
- [x] 持久化普通操作免审批开关，授权项目默认自动排队执行
- [x] 新增受管部署目标/服务器删除工具，强制参数绑定、审批和审计
- [x] 为完成任务生成结构化执行报告，要求 Agent 明确说明动作、位置、结果与验证
- **状态：** complete

### 阶段 56：稳定启动器与 AI 控制中心交互
- [x] 将三客户端和通用配置切换到应用数据目录中的稳定 MCP 启动器
- [x] 兼容已有直连可执行文件配置并提供 needsRepair 迁移提示
- [x] 增加审批策略开关、破坏性边界说明、开发路径提示和执行结果展示
- **状态：** complete

### 阶段 57：边界测试与交付
- [x] 覆盖免审批、无授权、授权变化、破坏性强制审批、删除依赖和幂等边界
- [x] 覆盖开发/正式路径迁移、路径含空格、旧配置修复、启动器权限与缺失可执行文件
- [x] 执行 MCP、服务端、Vue、Vite、Rust 与 diff 验证，记录真实远程删除未执行边界
- **状态：** complete

### 阶段 58：多用户/多设备基线与迁移契约
- [x] 盘点登录、Agent SQLite、中央部署库、整库同步、共享 Token 与固定密钥现状
- [x] 定义稳定的 account/device/team 上下文、数据库迁移版本与旧数据只读策略
- [x] 为中央及本机数据增加租户字段、约束和 legacy-disabled 迁移
- **状态：** complete

### 阶段 59：设备身份、系统钥匙串与短期会话
- [x] 生成永久 deviceId 与 Ed25519 设备密钥对，并存入系统安全凭据库
- [x] GitLab 身份换取短期访问令牌和设备绑定刷新令牌
- [x] 登录、刷新、登出、账号切换与设备撤销完成内存凭据清理和任务失效
- **状态：** complete

### 阶段 60：中央多租户、团队与 RBAC
- [x] 新增用户、设备、团队、成员、绑定、会话、密钥、操作和审计表
- [x] 为部署资源增加 team_id，并在 API 层统一校验团队、角色和资源归属
- [x] 旧共享 Token 写接口进入只读兼容，中央凭据按团队加密存储
- **状态：** complete

### 阶段 61：本机 Agent/MCP 账号设备隔离
- [x] Agent Gateway 注入 accountId、deviceId、teamId、客户端与请求标识
- [x] 项目授权、审批策略、计划、幂等任务和审计按账号与设备隔离
- [x] 中央离线时仅开放本地只读诊断；写操作统一返回可操作错误
- **状态：** complete

### 阶段 62：取消整库同步与本机构建/中央部署
- [x] 将“同步测试环境数据”替换为按团队刷新脱敏配置
- [x] 禁止桌面端调用原始数据库备份/恢复接口
- [x] 实现产物任务、分块断点上传、哈希校验、中央部署与操作轮询
- **状态：** complete

### 阶段 63：安全迁移与 AI 控制中心
- [x] 移除固定 DEPLOY_SECRET_KEY、正式包共享 Token 与 localStorage GitLab Token
- [x] 增加设备列表/撤销、当前账号团队、权限与迁移状态展示
- [x] destructive 操作落实 admin、名称二次匹配、参数绑定和强制审批
- **状态：** complete

### 阶段 64：多用户多设备测试与交付门禁
- [x] 覆盖双设备、双账号、双团队、RBAC、撤销、断网、并发锁和凭据泄漏场景
- [x] 覆盖迁移幂等、产物断点/哈希/超时、MCP structuredContent 与 stdout 纯净性
- [x] 执行 Vue、服务端、MCP、Rust 和打包前检查，记录真实多人/远程测试边界
- **状态：** complete（代码与自动化；真实 GitLab 双用户/双设备/测试服务器验收仍是发布前外部门禁）

### 阶段 65：免费本地保险库设计与兼容边界
- [x] 复核 macOS 钥匙串、设备身份、本机数据库主密钥与活动账号的依赖关系
- [x] 设计不读取旧钥匙串的本地加密保险库、文件权限和原子写入协议
- [x] 明确已有服务器凭据不可直接继承，升级后重新登录/重新录入敏感配置的行为
- **状态：** complete

### 阶段 66：跨平台安全存储实现
- [x] macOS 切换为应用数据目录下的本地加密保险库，启动过程不访问钥匙串
- [x] Windows 保持 Credential Manager 分项存储逻辑不变
- [x] 初始化存储目录并补齐加密、篡改、权限与持久化测试
- **状态：** complete

### 阶段 67：免费发布流程回退
- [x] 移除 GitHub Actions 对 Apple Developer Secrets、Developer ID 和公证的强制要求
- [x] 恢复 macOS ad-hoc 构建验收，同时保留 Tauri Updater Minisign 验签
- [x] 同步 README 与项目发布/更新 Skills，明确免费方案和用户迁移步骤
- **状态：** complete

### 阶段 68：macOS/Windows 验证与交付
- [x] 运行 Rust fmt/check/test、Vue 类型检查、服务端测试和前端生产构建
- [x] 检查 macOS 产物不触发钥匙串访问，并验证 Windows 条件编译保持原凭据库路径
- [x] 检查 staged/unstaged 差异，保护用户现有修改且不提交、不推送
- **状态：** complete

### 阶段 69：中央旧数据归属修复设计
- [x] 复现本机 3100/3101 有 9 条 legacy-team 数据而登录页面中央 v2 为空
- [x] 核对账号空间选择顺序、会话自动纠正和 team_id 密文 AAD 边界
- [x] 设计只绑定账号、不改业务数据 team_id 的幂等接管规则
- **状态：** complete

### 阶段 70：单账号旧空间自动接管实现
- [x] 中央服务识别“仅一个有效账号 + 存在 legacy 数据 + 无其他 owner”的安全场景
- [x] 将唯一账号绑定为 legacy-team 管理员，并让现有访问会话即时切换
- [x] 保留多账号/已有 owner 的隔离边界，禁止任意账号抢占旧数据
- **状态：** complete

### 阶段 71：迁移回归测试
- [x] 覆盖先创建个人空间、后出现 legacy 数据时自动接管
- [x] 覆盖两台设备共享 legacy-team、第二账号不可抢占、重复调用幂等
- [x] 验证目标列表可见且旧 team_id/密文归属不被改写
- **状态：** complete

### 阶段 72：全量验证与交付
- [x] 运行服务端测试、Vue 类型检查、生产构建与 diff 检查
- [x] 核对本地开发和已发布客户端均通过中央 v2 获得同一数据空间
- [x] 保护现有 NginxDeployActions 用户改动，不提交、不推送
- **状态：** complete

## 已做决策
| 决策 | 理由 |
|------|------|
| 本机构建、服务器只运行 | 与现有 Git 工作区/上传架构一致，避免污染目标机 |
| systemd 优先、PID 脚本兜底 | 兼顾可靠性和 guest 无 sudo 环境 |
| Gateway 全生命周期、Nacos 只监测 | 避免误重启共享注册中心 |
| OpenAPI 文件落盘、SQLite 只存元数据 | 避免大 JSON 膨胀数据库 |
| 保留现有路由和前端/Nginx 行为 | 控制改动范围并兼容已有用户数据 |
| 更新加速优先解决内网缓存而非客户端多线程 | 当前瓶颈位于 GitHub 回源，局域网静态 Range 下载无需增加分片复杂度 |
| Tauri 更新包验签与系统代码签名分离 | Tauri 自有公私钥免费；Apple/Windows 商业证书不纳入本次实现 |
| 双分支保持既有职责 | GitHub 分支构建客户端 Release，yuyan-3.0 分支仅部署内网 API/Web |
| 自动检查全程静默到本机安装包校验完成 | 避免把服务端准备和本机下载的技术过程暴露给用户 |
| 静默下载完成后不自动安装 | 安装会退出 App，必须由用户明确点击授权 |
| 手动检查保持即时反馈 | 用户主动操作不能表现为无响应，准备和失败状态需要明确说明 |
| 本机缓存状态绑定版本、资源身份和文件元数据 | 防止连续发布时把旧包误认为新版本，并支持重启恢复 |
| 保留自研下载、安装阶段接入官方 Updater | 继续获得内网缓存、断点和跨进程恢复，同时用官方平台安装逻辑完成覆盖与自动重启 |
| Updater 使用 yuyan-app 独立信任链 | 独立加密私钥进入 GitHub Actions Secrets，客户端只内置对应公钥，避免与 yuyan-vpn 的历史密钥混用 |
| 安装前重新查询有效版本 | 已撤回版本立即清理；出现更高版本时切换目标而不安装旧包 |
| 轮询结果使用代次隔离 | 防止旧版本服务器状态或原生状态迟到覆盖当前更新流程 |
| 后台命令按平台隐藏窗口而不丢弃输出 | Windows 使用进程创建标志隐藏控制台，继续保留 stdout/stderr 管道、退出码和诊断日志 |
| legacy-team 仅允许唯一有效账号自动接管 | 兼顾现有单账号数据恢复与多账号场景防抢占；已有 owner、GitLab 绑定或个人业务数据时不自动切换 |
| 保留 legacy-team 业务数据归属 | SSH/Nacos 凭据把 teamId 写入 AES-GCM AAD，改行归属会使密文失效；改成员和会话即可恢复可见性 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| deploy-store 目标查询大补丁上下文不匹配 | 1 | 改为按 map/list/get 三个小补丁应用 |
| backend-runtime PID 脚本模板中的 shell 参数被 JS 插值 | 1 | 将 `${1:-status}` 转义为模板字面量并重新语法检查 |
| src/api/deploy.ts JDK 大补丁尾部上下文不匹配 | 1 | 读取实际尾部后拆分接口与方法补丁 |
| 扩展任务类型后旧发布进度类型假设失效 | 1 | 在旧弹窗中显式收窄发布/回滚任务，并对联合结果完整去重 |
| 新增领域测试首次发现路径错误文案断言不一致和 Authorization 尾值未脱敏 | 1 | 修正断言，并优先整段脱敏 Authorization 头 |
| Java 管理旧扫描漏掉 SDKMAN，且运行环境只存路径 | 1 | 新增多来源本机扫描与 v3 服务器运行 JDK 外键关联 |
| 自动更新规划大补丁上下文不匹配 | 1 | 读取实际文件后按计划、发现和进度拆分补丁 |
| Codex 官方手册抓取缺少校验响应头 | 1 | 改用本机安装内容、Sparkle 与 Tauri 官方文档完成更新链路调研 |
| 工作区 pnpm 受系统 Node 12 与依赖状态检查影响 | 2 | 使用工作区 Node 直接执行 `node --test`，避免触发依赖重装 |
| 静默更新计划补丁目标行空格不匹配 | 1 | 使用文件中的实际文本重新应用小范围补丁 |
| 原生首轮 `cargo fmt --check` 发现新增代码排版差异 | 1 | 运行 `cargo fmt` 后再执行编译检查 |
| `open_path` 不接受 `PathBuf` | 1 | 转为受控缓存路径的字符串后调用 opener |
| Web 视觉验收运行时缺少 Python Playwright | 1 | 仅安装测试运行时模块，不加入项目依赖或应用产物 |
| Python Playwright 1.61.1 不存在且 PyPI SSL 中断 | 1 | 改用可用的 1.61.0，并对测试依赖下载使用可信 PyPI 主机参数 |
| Playwright 点击胶囊受既有 hover 位移动画影响持续判定不稳定 | 1 | 改用 Enter 键打开弹层，同时验证新增键盘交互 |
| 弹层测试命中 Ant Popover 的隐藏测量副本，按钮被判断不可见 | 1 | 将选择器收窄到 `:visible` 弹层后再验证按钮角色 |
| 原生收尾补丁受 rustfmt 后的无分号返回格式影响未匹配 | 1 | 按实际行号拆分应用取消轮询和遗留清理补丁 |
| 文件规划完成检查脚本没有执行权限 | 1 | 使用 `sh scripts/check-complete.sh` 显式执行 |
| 视觉验收系统 Python 与 Codex bundled Python 均缺少 Playwright | 1 | 改用 bundled Node 浏览器自动化依赖或现有 Chrome 控制能力，不写入项目依赖 |
| Playwright 基线连接 1420 后未渲染应用 | 2 | 已排除端口残留，第二次改为读取浏览器控制台、失败请求与 `#app` 内容，不再重复等待选择器 |
| 性能脚本 `wait_for_function` 参数位置不兼容 | 1 | 改用 Playwright Python API 的关键字参数 `arg=` |
| 菜单 hover/focus 预加载导致首次点击更慢 | 1 | 生产复测发现共享依赖解析会抢占 click 前主线程，已撤销预加载，保留点击先绘制再导航 |
| KeepAlive 验收脚本选中只读 Select 搜索框 | 1 | 将选择器收窄为可见且非 readonly/disabled 的输入框 |
| 规划文件多文件补丁包含空 hunk | 1 | 删除空 hunk 并拆成标准多文件补丁重新应用 |
| 发布历史视觉脚本在 Monaco 日志内容异步渲染前断言失败 | 3 | 改为等待可见抽屉出现 ERROR 与退出码/fatal 文本后再截图和断言 |
| 新增目标视觉脚本缺少 GitLab 项目数据，弹窗按业务规则自动关闭 | 1 | 仅在浏览器验收层拦截项目/分支接口返回最小测试数据，不修改产品代码 |
| OpenAPI JDK 绑定验证脚本的 Java 版本正则过度转义，首次解析为 0 | 1 | 改用与产品一致的主版本数字解析后重新扫描并绑定 |
| 浏览器验收用文字节点/Playwright 合成点击未触发 Ant Modal 的异步确认回调 | 2 | 改为调用可见确认按钮原生 `click()`，完整跑通扫描、更新与二次生成链路 |
| JSON Monarch 兜底首次注册后被 Monaco 内置 JSON contribution 异步覆盖 | 2 | 为 OpenAPI 预览注册独立 `openapi-json` 语言，隔离内置 language service 的加载时序 |
| 侧栏规划文件首个多文件补丁未匹配 `progress.md` 标题 | 1 | 读取真实标题后拆分为小补丁，避免长文件上下文互相影响 |
| 侧栏首轮静态检查被系统 Node 12 拦截 | 1 | 不重复调用默认 pnpm，改用工作区已配置的新版 Node 执行 pnpm 入口 |
| 工作区 pnpm 包装器在非 TTY 触发依赖目录清理检查 | 1 | 不设置 CI 强制安装，直接用新版 Node 执行现有 vue-tsc 与 Vite 入口，保护 node_modules |
| 侧栏 Less 首次构建提示顶层属性缺少选择器 | 1 | 将导入样式中的裸 `&` 改为显式 `.ant-layout-sider` 根选择器后重新构建 |
| bundled Python 视觉脚本缺少 Playwright | 1 | 不向项目安装测试依赖，改用运行时自带的浏览器自动化包完成截图与交互验收 |
| 应用内浏览器不支持 `networkidle` 等待 | 1 | 改为等待 `load` 并以 `.ant-layout-sider` 可见作为稳定信号 |
| 暗色首张截图停留在设置抽屉关闭动画帧 | 1 | 不把临时遮罩/溢出归因于侧栏，等待 DOM 稳定后重新测量并截图 |
| 等待已关闭的设置标题时定位数量为 0 | 1 | 获取新 DOM 快照确认抽屉已卸载，直接对稳定页面重新测量，不重试旧定位器 |
| 四张反馈图首次转发时图片 helper 接收到数组索引 | 1 | 改用显式箭头函数只传图片对象，不重复错误调用方式 |
| 主界面截图前开屏动画自动结束导致跳过按钮数量为 0 | 2 | 获取新 DOM 快照后直接验收稳定主界面，不重试已失效定位器 |
| 折叠态收敛的多文件补丁 hunk 分隔符无效 | 1 | 未应用代码，拆分为 CSS 与规划记录两个标准小补丁 |
| 热更新后旧“展开侧栏”定位数量为 0 | 1 | 获取新 DOM 快照确认页面已重置为展开态，以最新语义状态继续验收 |
| scoped Less 中暗色 Header 全局祖先选择器未实际覆盖亮色变量 | 1 | 由 `useTheme` 显式绑定 `is-dark` 类，暗色样式稳定命中并截图复验 |
| 浏览器封装不支持 `waitForSelector` 和伪元素 locator | 2 | 使用新 DOM 快照与真实元素类名计数确认状态，不继续调用不支持的接口 |
| 浏览器标签 API 不提供 `tabs.open` | 1 | 按支持的接口改用 `tabs.new()` 后调用 `tab.goto()`，未重复错误调用 |
| 本机 yuyan-vpn updater 公钥与仓库已发布公钥不一致 | 1 | 禁止使用本机私钥签正式包；客户端固定信任已发布公钥，CI 明确校验并要求配置对应线上 Secret |
| 首轮 `cargo fmt --check` 报告 2 处测试代码排版差异 | 1 | 运行 `cargo fmt` 进行纯机械格式化后继续 Rust 编译测试 |
| 完整本地 Tauri 打包在 updater 签名阶段被拦截 | 1 | 已确认前端、Rust release 和 `.app.tar.gz` 均构建成功；本机缺少与固定公钥匹配的私钥，保留安全门禁并由 CI Secret 完成真实签名 |
| 复用本机 VPN 私钥实签失败 | 1 | 私钥既需要未知密码又与已发布 VPN 公钥不匹配；改为生成 yuyan-app 专用加密密钥并独立托管 |
| macOS `base64 --decode <file>` 参数格式不兼容 | 1 | 改用 BSD `base64 -D -i <file>` 读取公开 key id，不重复错误命令 |
| 本地保险库首轮 `cargo fmt --check` 报告新增 Rust 长行排版差异 | 1 | 运行 `cargo fmt` 做纯机械格式化后再执行编译和测试 |
| CI #128 三平台均提示 updater 私钥 base64 格式无效 | 1 | 用新生成的 yuyan-app 加密私钥覆盖 GitHub Secret，并新增配套密码 Secret |
| 直接把配置公钥解码后与 `.pub` 文件逐字节比较失败 | 1 | `.pub` 本身就是 Tauri 使用的 base64 文本；改为规范化文本比较，并以真实产物签名验签作为最终依据 |
| `yuyan-3.0` worktree 默认 Node 12 不支持 `--test` 且没有独立依赖目录 | 2 | 显式使用 Node 22，并临时链接主工作区依赖完成 18 项更新测试，随后移除链接 |
| GitHub `releases/latest` 对 prerelease 返回 404 | 1 | 查询 Release 列表并验证最新的 `v1.2.16` prerelease 及全部资产 |
| Windows 闪窗首个多文件补丁未匹配 deploy-service 导入位置 | 1 | 确认补丁未部分应用，按 Rust、公共工具和各调用文件拆分小补丁 |
| 系统默认 Node 12 无法运行当前 pnpm | 1 | 使用 Codex 工作区 Node 22 运行服务端测试与现有依赖入口 |
| 工作区 pnpm 包装器在非 TTY 下尝试清理依赖目录并中止 | 1 | 不改依赖，直接用 Node 22 执行现有 vue-tsc 与 Vite 入口 |
| macOS 交叉检查 Windows target 被 ring 缺少 MSVC `assert.h` 阻断 | 1 | 保留本机原生完整编译测试，并用 Windows target 最小 Rust 编译验证 `CommandExt::creation_flags` 代码 |
| 本机完整 Windows target 检查被 ring 缺少 MSVC `assert.h` 阻断 | 1 | 不重复交叉构建；核对 Windows 条件编译仍走原 Credential Manager，并以 GitHub Actions Windows runner 为最终平台构建门禁 |
| 系统 Ruby 2.6 的 `YAML.load_file/load` 不接受 `aliases:` 关键字参数 | 2 | 工作流不使用 YAML alias，改用普通 `YAML.load(File.read(...))` 完成语法解析，不修改系统 Ruby |
| 在 `src-tauri` 工作目录读取仓库级 workflow 路径失败 | 1 | Rust 全量测试不受影响；后续仓库级检查统一在项目根目录执行 |
| legacy 接管首个回归用例在写入旧服务器前已进入 legacy-team | 1 | 检查新库各根表默认记录，收窄“业务数据存在”的判定，避免由自动发现的本机资源误触发接管 |

## 备注
- 当前工作区已有用户改动，所有修改必须基于现状增量完成。
- 每完成一个阶段更新本文件和 progress.md。
