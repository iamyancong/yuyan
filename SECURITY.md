# 安全策略与声明 (Security Policy)

雨燕（Yuyan）作为一站式前端工程化与运维部署平台，高度重视数据安全与系统机密性保护。

## 🔒 凭据加密与存储架构

1. **零明文存储**：
   - 目标主机的 SSH 登录密码、私钥、Nacos 密码及 GitLab Token 绝不以明文形式存储。
   - 所有敏感凭据入库前均经过 `AES-256-GCM` 认证加密，每次加密生成独立 IV 并绑定租户 AAD（附加认证数据）。
2. **设备/环境独立主密钥**：
   - 本机桌面端从平台原生安全加密保险库（macOS）或安全存储（Windows）读取主密钥。
   - 源码与二进制安装包中**严禁包含共享默认密钥**。
3. **数据主权红线 (Data Sovereignty)**：
   - **`deploy.sqlite` 数据库文件包含内网服务器资产拓扑与加密凭据，严禁上传至公网 GitHub 仓库或公开 Release。**
   - 数据库必须严格保留在使用者本机或企业私有内网服务器中。

## 🛡️ 支持的版本

| 版本系列 | 支持状态 |
| :--- | :--- |
| 1.2.x | ✅ 活跃维护与安全修复 |
| < 1.2.0 | ❌ 已废弃，建议升级 |

## 🚨 漏洞报告 (Reporting a Vulnerability)

如果您在雨燕中发现了任何潜在的安全漏洞、敏感信息泄露风险或加密实现缺陷：

1. **请勿直接在 GitHub 上公开提 Issue**，以免在漏洞修复前引发恶意利用。
2. 请发送邮件至项目安全维护团队（或作者联系邮箱），并在邮件中注明：
   - 漏洞影响的功能模块与版本
   - 复现步骤或 PoC 概念验证说明
   - 潜在影响评估
3. 我们承诺在收到报告后 48 小时内完成评估并给出响应，并在修复完成后发布安全补丁更新。
